# masitcon Zeiterfassung (Ameise) - Caddy-Snippet (Template)
#
# NICHT direkt verwenden! deploy.sh generiert das echte Snippet
# mit korrekter Domain unter: /opt/projects/.../configs/caddy-snippet.conf
#
# Manuelle Nutzung:
#   1. DOMAIN ersetzen (z.B. zeiterfassung.example.com)
#   2. In /etc/caddy/Caddyfile einfügen oder nach /etc/caddy/conf.d/ kopieren
#   3. sudo systemctl reload caddy

# DOMAIN {
#     reverse_proxy 127.0.0.1:8080
#     log {
#         output file /var/log/masitcon-tools-ameise/caddy-access.log
#     }
# }
